<% @artworks.each do |artwork| %>
  <%= link_to artwork do %>
    <img src="<%= artwork.image %>" alt="<%= artwork.title %> Image">
  <% end %>
  bookings for this peice:<br>
  <% artwork.bookings.each do |booking| %>
    <%= booking.message %>
  <% end %>
<%# iterate through bookings for artwork here %>
<% end %>
<br>
Your bookings:
<% @bookings.each do |booking| %>
  <%# <%= link_to booking.message, booking %><br>
  <%= booking.message %>
<% end %>
